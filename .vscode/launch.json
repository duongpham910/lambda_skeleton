{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to SAM CLI",
            "port": 5858,
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/var/task",
            "protocol": "inspector",
            "stopOnEntry": false
        }
    ]
}
